package com.techelevator.candy;

public class SourCandy extends Candy{
    //Constructor
    public SourCandy(String ID, String name, boolean isWrapped, int quantity, double price) {
        super(ID, name, isWrapped, quantity, price);
    }
}
