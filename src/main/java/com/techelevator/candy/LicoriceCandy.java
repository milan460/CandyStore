package com.techelevator.candy;

public class LicoriceCandy extends Candy{

    //Constructor
    public LicoriceCandy(String ID, String name, boolean isWrapped, int quantity, double price) {
        super(ID, name, isWrapped, quantity, price);
    }
}
